<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DesarrolloMovil"
        tools:targetApi="31">

        <!-- Activity principal (pantalla de inicio) -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activities de Autenticación -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Activities Principales -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".PanelDocenteActivity"
            android:exported="false" />

        <!-- Activities de Perfil y Configuración -->
        <activity
            android:name=".PerfilDocenteActivity"
            android:exported="false" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".NotificacionesActivity"
            android:exported="false" />

        <!-- Activities de Estudiantes -->
        <activity
            android:name=".ListaEstudiantesActivity"
            android:exported="false" />
        <activity
            android:name=".DetalleEstudianteActivity"
            android:exported="false" />

        <!-- Activities de Validación IA -->
        <activity
            android:name=".SeleccionarCursoValidacionActivity"
            android:exported="false" />
        <activity
            android:name=".ValidacionPreguntasActivity"
            android:exported="false" />

        <!-- Activities de Asignaturas -->
        <!-- Activities de Asignaturas -->
        <activity
            android:name=".CrearCursoActivity"
            android:exported="false" />

        <activity
            android:name=".ListaCursosActivity"
            android:exported="false" />

        <!-- Activities de Gestión de Grupos -->
        <activity
            android:name=".GestionGruposActivity"
            android:exported="false" />
        <activity
            android:name=".AsignarEstudiantesActivity"
            android:exported="false" />
        <activity
            android:name=".VisualizarGruposActivity"
            android:exported="false" />


        <activity
            android:name=".UsuariosAsignadosActivity"
            android:exported="false"
            android:label="Usuarios Asignados" />

    </application>

</manifest>
